import React, { useState } from "react";
import { View } from "react-native";
import { Text } from "react-native-elements";

export default function Winners({ navigation }) {
  return (
    <View>
      <Text h1> Winners!</Text>
    </View>
  );
}
